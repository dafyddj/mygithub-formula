# -*- coding: utf-8 -*-
# vim: ft=yaml
---
mygithub:
  pkg:
    name: mygithub
  rootgroup: root
  config: '/etc/mygithub'
  service:
    name: mygithub
  subcomponent:
    config: '/etc/mygithub-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
